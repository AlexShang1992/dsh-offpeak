## What does this PR do?

<!-- One or two sentences. Link the issue it closes, if any. -->

Closes #<!-- issue number -->

## Checklist

- [ ] `pnpm run check` passes locally (typecheck + test + lint + build)
- [ ] Tests cover the changed behavior (boundary times for pricing, durability for storage)
- [ ] User-facing copy is added in **both** English and Simplified Chinese (`src/client/locales.ts`)
- [ ] README updated (both languages) if behavior or settings changed
- [ ] No literal colors or inline styles in components — CSS lives in `src/client/styles.ts`
- [ ] New Remote methods (if any) have zod codecs, Typert invocation descriptors, and a manifest member

## Screenshots (for UI changes)

<!-- Paste screenshots of the changed UI, light and dark if relevant. -->

## Notes for reviewers

<!-- Anything non-obvious: design decisions, edge cases, follow-ups. -->
